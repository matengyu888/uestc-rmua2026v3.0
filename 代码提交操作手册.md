# RoboMaster 2026 无人机赛项代码提交操作手册（按命令执行）

本文基于以下两份材料整理：
- `/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/README.md`
- `/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase/UTF-8__RoboMaster 2026 机甲大师高校人工智能挑战赛比赛规则手册V1.0.0.pdf`

假设你已经完成算法开发，这里只讲“如何把代码做成可提交镜像并上传”。

## 0. 进入项目目录

```bash
cd /home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control
```

## 1. 进入 Docker 构建目录

```bash
cd /home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev
```

## 2. 构建提交镜像

```bash
docker build -t rmua2026_uav:final .
```

## 3. 本地自检：镜像是否能自动启动程序

规则要求镜像部署后要能运行，且不能主动退出。先本地跑一遍：

```bash
docker rm -f rmua2026_check 2>/dev/null || true
```

```bash
docker run -it --net host --name rmua2026_check --rm rmua2026_uav:final
```

说明：
- 你应看到程序自动启动（本项目 `ENTRYPOINT` 是 `/setup.bash`，会执行 `rosrun basic_dev basic_dev`）。
- 观察程序是否稳定运行、不会秒退。
- 验证后按 `Ctrl+C` 结束本地测试。

## 4. 自检：不要设置禁用环境变量（README 要求）

README 明确要求不要在启动文件里提供 `ROS_IP` 和 `ROS_MASTER_URI`。

```bash
cd /home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control
```

```bash
rg -n "ROS_IP|ROS_MASTER_URI" basic_dev
```

如果命中了你自己新增的启动脚本/配置，请删除这些变量后再重新构建镜像。

## 5. 自检：镜像大小必须小于 15G（规则硬性要求）

```bash
docker image inspect rmua2026_uav:final --format '{{.Size}}' | awk '{printf "Image size: %.2f GB\n", $1/1024/1024/1024}'
```

如果输出大于 `15.00 GB`，需要瘦身后重新构建。

## 6. 导出提交包（tar）

```bash
cd /home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev
```

```bash
EXPORT_NAME="rmua2026_uav_final_$(date +%Y%m%d_%H%M%S).tar"
```

```bash
docker image save rmua2026_uav:final -o "$EXPORT_NAME"
```

```bash
ls -lh "$EXPORT_NAME"
```

## 7. 生成校验值（建议）

```bash
sha256sum "$EXPORT_NAME" > "$EXPORT_NAME.sha256"
```

```bash
cat "$EXPORT_NAME.sha256"
```

## 8. 上传到比赛系统

上传文件：
- `basic_dev/$EXPORT_NAME`

上传后建议记录：
- 提交时间
- 镜像 tag（`rmua2026_uav:final`）
- tar 文件名
- sha256

## 9. 提交规则关键点（务必满足）

以下来自规则手册中“比赛流程/镜像规范”：

1. 每队每天最多提交 10 次。
2. 每天首次提交有优先运行权限。
3. 同一时间原则上只能有一个镜像排队等待运行；若排队中再次上传，新镜像会替换未运行镜像并重新排队。
4. 镜像必须小于 15G。
5. 镜像部署成功后 30 秒内，无人机需离开起点触发区域，否则本次运行失败。
6. 镜像运行后不可主动退出。
7. 镜像不支持 GUI（如 X11、RViz 可视化界面等）。

## 10. 一次性命令清单（从构建到导出）

如果你希望从头到尾直接跑一遍，按下面顺序执行：

```bash
cd /home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev
docker build -t rmua2026_uav:final .
docker rm -f rmua2026_check 2>/dev/null || true
docker run -it --net host --name rmua2026_check --rm rmua2026_uav:final
# 本地观察启动是否正常后 Ctrl+C 结束
cd /home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control
rg -n "ROS_IP|ROS_MASTER_URI" basic_dev
docker image inspect rmua2026_uav:final --format '{{.Size}}' | awk '{printf "Image size: %.2f GB\n", $1/1024/1024/1024}'
cd /home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev
EXPORT_NAME="rmua2026_uav_final_$(date +%Y%m%d_%H%M%S).tar"
docker image save rmua2026_uav:final -o "$EXPORT_NAME"
ls -lh "$EXPORT_NAME"
sha256sum "$EXPORT_NAME" > "$EXPORT_NAME.sha256"
cat "$EXPORT_NAME.sha256"
```

执行完后，把生成的 tar 上传到比赛系统即可。



**结论**
按当前启动脚本 [start_dp_planner.sh](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/start_dp_planner.sh#L12) 和主 launch [hybrid_dp_control.launch](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/controller/launch/hybrid_dp_control.launch#L1) 来看，当前实际主链路是：

`模拟器/真值位姿 -> HybridRouteManager 产出局部目标 /goal_pose`
`前视双目 + lidar -> StereoDepthNode 产出深度图`
`/goal_pose + pose_gt + depth -> DPPlanner 产出 /position_cmd`
`/position_cmd + pose_gt -> PositionController -> /airsim_node/drone_1/vel_body_cmd`

**1. 当前整体规划器/控制器基本链路**
1. 启动链路
   [start_dp_planner.sh](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/start_dp_planner.sh#L145) 会先起模拟器、等待起飞服务、发送起飞，再拉起 `hybrid_dp_control.launch`。

2. 路线管理层
   [hybrid_route_manager_node.cpp](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/planner/src/hybrid_route_manager_node.cpp#L81) 从 `rule_based_strict_route_anchors.csv` 读锚点，做路径加密、分段、进度跟踪，然后持续发布 `/goal_pose`。
   它不是直接输出控制量，而是给 DP 规划器一个“当前该朝哪段门/走廊飞”的局部目标。

3. 感知层
   [stereo_depth_node.cpp](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/planner/src/stereo_depth_node.cpp#L20) 同步左右前视图，跑 `StereoSGBM` 算深度，再把 lidar 点云投影融合进深度图，发布 `/airsim_node/drone_1/front_stereo/DepthPerspective`。

4. DP 规划层
   [dp_planner_node.py](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/planner/scripts/dp_planner_node.py#L30) 订阅：
   `pose_gt/odometry + depth + /goal_pose`
   然后用模型 [model.py](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/planner/scripts/model.py#L5) 推理，输出 `PositionCommand` 到 `/position_cmd`。

5. 控制层
   [pos_controller_node.cpp](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/controller/src/pos_controller_node.cpp#L48) 订阅 `/position_cmd` 和 `pose_gt`，做世界系位置 PID，转成机体系速度 `VelCmd`，发布到 `/airsim_node/drone_1/vel_body_cmd`。

**2. 当前主链路里每层做了什么**
1. `HybridRouteManager`
   [hybrid_route_manager_node.cpp](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/planner/src/hybrid_route_manager_node.cpp#L151) 读 CSV。
   [hybrid_route_manager_node.cpp](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/planner/src/hybrid_route_manager_node.cpp#L229) 按 `route_spacing` 加密路径。
   [hybrid_route_manager_node.cpp](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/planner/src/hybrid_route_manager_node.cpp#L260) 用“相邻锚点很近”识别门/分段。
   [hybrid_route_manager_node.cpp](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/planner/src/hybrid_route_manager_node.cpp#L394) 基于当前位置在当前段内找最近 raw/dense 点。
   [hybrid_route_manager_node.cpp](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/planner/src/hybrid_route_manager_node.cpp#L485) 生成局部目标点和目标朝向。
   [hybrid_route_manager_node.cpp](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/planner/src/hybrid_route_manager_node.cpp#L624) 满足到门/过门条件后切到下一段。

2. `StereoDepthNode`
   [stereo_depth_node.cpp](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/planner/src/stereo_depth_node.cpp#L127) 选时间最接近的左右图。
   [stereo_depth_node.cpp](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/planner/src/stereo_depth_node.cpp#L267) 计算视差和深度。
   [stereo_depth_node.cpp](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/planner/src/stereo_depth_node.cpp#L292) 融合 lidar。
   [stereo_depth_node.cpp](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/planner/src/stereo_depth_node.cpp#L305) 中值滤波后发布深度。

3. `DPPlanner`
   [dp_planner_node.py](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/planner/scripts/dp_planner_node.py#L289) 只有在位姿、目标、深度都齐了才工作。
   [dp_planner_node.py](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/planner/scripts/dp_planner_node.py#L313) 先按目标方向构造一个目标速度 `target_v_world`。
   [dp_planner_node.py](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/planner/scripts/dp_planner_node.py#L322) 再结合当前航向做转弯减速/倒车朝向判断。
   [dp_planner_node.py](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/planner/scripts/dp_planner_node.py#L355) 把当前速度、目标速度、固定 up 轴、margin 和深度特征喂给网络。
   [dp_planner_node.py](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/planner/scripts/dp_planner_node.py#L369) 输出 `PositionCommand`：位置前视点、目标速度、预测加速度、yaw。

4. `PositionController`
   [pos_controller_node.cpp](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/controller/src/pos_controller_node.cpp#L192) 周期控制。
   [pos_controller_node.cpp](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/controller/src/pos_controller_node.cpp#L239) 对 `position.x/y/z` 做 PID。
   [pos_controller_node.cpp](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/controller/src/pos_controller_node.cpp#L251) 用规划器给的 `velocity` 只做“速度上限约束”，不做前馈叠加。
   [pos_controller_node.cpp](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/controller/src/pos_controller_node.cpp#L270) 世界系转机体系。
   [pos_controller_node.cpp](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/controller/src/pos_controller_node.cpp#L280) yaw 用 `yaw_dot + kp * yaw_err`，再限幅，发布 `VelCmd`。

**3. 当前代码里加入的“其他约束”统一汇总**
1. 路线/赛道约束
   `CSV 锚点约束`：路径以 [rule_based_strict_route_anchors.csv](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/sim_tools/path/rule_based_strict_route_anchors.csv) 为基准。
   `分段约束`：相邻 raw 点距离小于 `duplicate_gate_threshold` 视为门边界，形成 segment。
   `段内单调前进约束`：最近点搜索只在当前进度后面的窗口内找，不回头扫全路径。
   `高度弱化约束`：进度匹配时 z 误差乘 `progress_z_weight`，说明更重视平面进度。

2. 过门/段切换约束
   `入口保持约束`：进入新段时，如果还没真正到 entry gate，就先把局部目标钉在入口门上。
   `近门减速/收敛约束`：靠近当前 gate 时，局部目标直接切到 gate，而不是继续走廊前视。
   `切段判定约束`：满足“到门半径”或“沿门法向已经穿过去”才切下一段。
   `目标重发约束`：目标变化不够大时不重复发，除非已经接近当前目标。

3. 感知约束
   `双目同步约束`：左右图时间差必须小于 `max_sync_dt`。
   `lidar 同步约束`：点云与图像时间差必须小于 `lidar_sync_dt`。
   `深度范围约束`：深度裁剪到 `[min_depth_m, max_depth_m]`。
   `相机退化保护`：缺失 `CameraInfo` 时用 `fallback_width/height/fov` 估计内参。
   `lidar 覆盖约束`：投影进图像后用 patch 半径填补局部深度空洞。

4. DP 规划约束
   `最大速度约束`：`max_speed=16`，竖直 `max_speed_z=2.5`，见 [dp_planner.launch](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/planner/launch/dp_planner.launch#L1)。
   `目标到达约束`：若启用 `stop_at_goal`，距离小于 `goal_reached_dist` 就停规划。
   `位置前视约束`：不是直接把最终目标发给控制器，而是发 `cmd_pos_horizon_sec/cmd_pos_horizon_z_sec` 对应的前视位置。
   `转弯减速约束`：航向误差超过 `turn_slowdown_start_deg` 后按比例降速。
   `大角度掉头约束`：误差超过 `turn_reverse_trigger_deg` 且反向朝向更优时，允许按 `reverse_turn_speed_scale` 低速“倒着过渡”。
   `yaw 变化率约束`：目标 yaw 不是瞬时跳变，而是按 `yaw_slew_rate` 逐步逼近。
   `网络安全边界参数`：`margin` 和 `thr_est_error` 参与网络状态/加速度修正。

5. 控制器约束
   `PID 输出限幅`：XY、Z 都有 `max_out/max_integral`。
   `速度封顶约束`：控制器实际速度不能超过规划器 `velocity` 给出的模长。
   `命令超时约束`：超过 `cmd_timeout=0.3s` 就清 PID 并发零速。
   `起步小参数约束`：先用 startup PID，小范围飞行后再平滑过渡到正常 PID，参数在 [pos_params.yaml](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/controller/config/pos_params.yaml#L1)。
   `yaw 角速度限幅`：普通跟踪和纯旋转模式有不同 `max_yaw_rate`。
   `坐标系符号约束`：当前统一把 NED 的 z 乘 `-1` 转成 z-up 再控。
   `机体加速度档位约束`：`VelCmd.va` 取 `xy_accel_limit`。

6. 运行时约束
   `起飞后再接管`：脚本里是先起飞，再等 2 秒，再启动规划控制链。
   `进程存活约束`：模拟器和 launch 任一退出都会触发报错/清理。

**4. 当前主链路里“实际上没用上”或“备用”的逻辑**
1. 旧链路/备用链路
   [dp_control.launch](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/controller/launch/dp_control.launch#L1) 是另一套：
   `path_loader -> local_goal_publisher -> dp_planner -> pos_controller`
   这是旧的“全局路径 + 局部目标”版本，不是当前默认启动链路。

2. 更老的路径跟随器
   [path_follower.cpp](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/planner/src/path_follower.cpp#L479) 和 [planner_switcher.py](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/planner/scripts/planner_switcher.py#L12) 还在仓库里，但当前 `hybrid_dp_control.launch` 没启用。

3. 控制器里的 `BRAKE_FOR_TURN/ROTATE_FOR_TURN`
   [pos_controller_node.cpp](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/controller/src/pos_controller_node.cpp#L50) 支持 `trajectory_flag=101/102` 的刹车/原地转向模式。
   但当前 [dp_planner_node.py](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/planner/scripts/dp_planner_node.py#L398) 只发 `TRAJECTORY_STATUS_READY=1`，所以这两个特殊模式在当前主链路里基本是“预留未触发”。

4. DP 输出的 `acceleration`
   [dp_planner_node.py](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/planner/scripts/dp_planner_node.py#L388) 会填加速度。
   但 [pos_controller_node.cpp](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/controller/src/pos_controller_node.cpp#L248) 当前并没有把它作为速度前馈或内环输入，只用了 `position/velocity/yaw/yaw_dot`。

**5. 一个需要单独注意的点**
当前控制器发布的是 `/airsim_node/drone_1/vel_body_cmd`，见 [pos_controller_node.cpp](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/basic_dev/src/controller/src/pos_controller_node.cpp#L61)；而 README 和基础示例写的是 `/airsim_node/drone_1/vel_cmd_body_frame`，见 [README.md](/home/uestc/XZC/RMUA/IntelligentUAVChampionshipBase_only_control/README.md#L91)。  
这说明“当前代码实现的话题名”和“文档/旧示例”存在不一致，属于后续联调时必须再确认的一项。

如果你愿意，我下一步可以继续帮你把这套链路再整理成一张“节点-话题-约束-参数”的总表。